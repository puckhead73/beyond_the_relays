@activation_cost = 150
@triumph_duration = 3600
r_btr_collector_relic = {
    activation_duration = @triumph_duration
    portrait = GFX_relic_btr_collector_relic
    sound = event_activating_unknown_technology

    resources = {
		category = relics
		# Activation cost
		cost = {
			influence = @activation_cost
        }
        produces = {
            energy = 30
        }
    }

    ai_weight = {
        weight = 100
    }

    triggered_country_modifier = {
		potential = {
			always = yes
        }
    }
    
    score = 500 # Almost as good as great khan

    active_effect = {
        custom_tooltip = btr_collector_relic_effect_tooltip
        hidden_effect = {
            country_event = {
                id = btr_collector.7            
            }
        }
		custom_tooltip = relic_triumph_cooldown
		hidden_effect = {
			add_modifier = {
				modifier = "relic_activation_cooldown" 
				days = @triumph_duration
			}		
		}
    }

    possible = {
		custom_tooltip = {
			fail_text = "requires_relic_no_cooldown"
			NOT = { has_modifier = relic_activation_cooldown }
        }
        custom_tooltip = {
            fail_text = btr_collector_relic_fail
        }
	}
}